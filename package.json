{
  "name": "scems-ios-wrapper",
  "version": "1.0.0",
  "description": "SCEMS Protocol iOS wrapper",
  "scripts": {
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
